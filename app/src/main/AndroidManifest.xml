<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" /> <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" /> <!-- uses-permission android:name="android.permission.INSTALL_PACKAGES"/ -->
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_daitech_logo"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_daitech_logo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <receiver
            android:name="ique.daitechagent.geofencing.GeofenceReceiver"
            android:enabled="true"
            android:exported="true" />

        <service
            android:name="ique.daitechagent.geofencing.GeofencingService"
            android:enabled="true"
            android:exported="true" />

        <receiver
            android:name="ique.daitechagent.service.CommentReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="ique.daitechagent.service.restartservice" />
            </intent-filter>
        </receiver>

        <service
            android:name="ique.daitechagent.service.CommentService"
            android:enabled="true"
            android:exported="true" />

        <activity android:name="ique.daitechagent.activities.ChatActivity" />

        <provider
            android:name="ique.softwareupdate.update.GenericFileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity
            android:name="ique.daitechagent.activities.MapsCustomerLocation"
            android:label="@string/title_activity_maps_customer_location" />
        <activity
            android:name="ique.daitechagent.activities.ScanHistory"
            android:label="@string/title_activity_scan_history"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="ique.daitechagent.activities.MainActivity"
            android:label="@string/app_name" />
        <!--
         service
            android:name=".service.LocationSharingService"
            android:enabled="true"
            android:exported="true"></service
        -->
        <activity
            android:name="ique.daitechagent.activities.Login"
            android:theme="@style/AppTheme.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="ique.daitechagent.activities.NetworkAvailability"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <receiver
            android:name="ique.daitechagent.service.TestReceiver"
            android:enabled="true"
            android:exported="true" />
        <receiver
            android:name="ique.daitechagent.service.NotificationReceiver"
            android:enabled="true"
            android:exported="true" />

        <service
            android:name="ique.daitechagent.service.LocationSharingService"
            android:label="Bus Tracking System - Driver"
            android:theme="@style/AppTheme.NoActionBar">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="ique.daitechagent.activities.MainActivity" />
        </service>
        <service
            android:name="ique.daitechagent.versioncheckservice.VersionCheckService"
            android:exported="false" />
        <service
            android:name="ique.softwareupdate.update.DownloadService"
            android:exported="true" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />
    </application>

</manifest>